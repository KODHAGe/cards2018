{
  "name": "xmas-card",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "is-url": "^1.2.4",
    "p5": "^0.7.2",
    "query-string": "^6.2.0",
    "rc-slider": "^8.6.3",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "prettier": "prettier --print-width 80 --no-semi --single-quote --trailing-comma es5 --write src/**/*.js",
    "now": "now ./build -A ../now.json && now alias",
    "deploy": "react-scripts build && npm run now"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "prettier": "^1.15.3"
  }
}
